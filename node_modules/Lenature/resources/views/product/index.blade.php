@extends('layouts.app')

@section('content')
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="row">

    <div class="col-md-12">

        <h1 class="m-0 text-dark col-md-6 float-left">Products</h1>

        <a class="btn btn-primary float-right btn-sm" href="/products/create"><i class="fa fa-plus" aria-hidden="true"> New</i> </a>
        <a class="btn btn-primary float-right btn-sm mr-2" href="/export-headers">Export </a>
        <button type="button" class="btn btn-primary float-right btn-sm mr-2" data-bs-toggle="modal" data-bs-target="#exampleModal">
            Import
        </button>
    </div>
</div>


                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>S.No</th>
                            <th>Product Name</th>
                            <th>Category</th>
                            <th>Price</th>
                            <th>Quantity</th>
                            <th>Status</th>
                            <th>Action</th>
                           
                           
                        </tr>
                    </thead>
                    <tbody>
                        @foreach ($items as $key => $item)
                        <tr>
                            <td>{{ $key + 1 }}</td>
                            <td><a href="/products/{{ $item->id }}" >{{ $item->product_name }}</a></td>
                            <td>{{$item->category->category_name}}</td>
                            <td>{{$item->final_price}}</td>
                            <td>{{$item->available_qty}}</td>
                            <td>{{$item->availablity_status}}</td>
                            <td><a href="/products/{{ $item->id }}/edit"  class="btn btn-success">Edit</a> 
                            <a href="/products/{{$item->id}}/delete" class="btn btn-danger">Delete</a></td>
                           
                        </tr>
                        @endforeach
                    </tbody>
                </table>

                <!-- modal pop up for import -->
                <div class="modal" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form action="/import-excel" method="POST" enctype="multipart/form-data">
          {{ csrf_field() }}
      <div class="modal-body">
            <input type="file" name="excel_file">
            
    
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <button type="submit" class="btn btn-primary">Import</button>
      </div>
      </form>
    </div>
  </div>
</div>


                




            </div>
        </div>
    </div>
</div>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

@stop
